#include <stdio.h>
#include <stdlib.h>

/** \brief Esta funcion carga valores enteros dentro de un vector de enteros.
 *
 * \param vector int* El puntero al vector de enteros
 * \param tam int El tamaño del vector de enteros
 * \return int Devuelve -1 si no pudo realizar la carga, caso contrario devuelve 0.
 *
 */
int cargarVector(int* vector, int tam);
/** \brief Esta funcion muestra los valores enteros dentro de un vector de enteros.
 *
 * \param vector int* El puntero al vector de enteros
 * \param tam int El tamaño del vector de enteros
 * \return int Devuelve -1 si no pudo rmostrar, caso contrario devuelve 0.
 *
 */
int mostrarVector(int* vector, int tam);
